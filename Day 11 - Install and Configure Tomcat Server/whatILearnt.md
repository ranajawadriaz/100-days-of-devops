I had to deploy a file on tomcat server. First the file was on another server so I copied it to the required one. Then I installed the java and tomcat on the required server but before that i took the admin priviledges. After the installation I configured the tomcat port from 8080 to 8089 for it to run on 8089 port. After this configuration I copied the ROOT.war file which I brought from another server into the tomcat webapps folder but before that I deleted the previos ROOT file. After that I started the tomcat server then enabled it and checked it using the curl http://stapp01:8089 command.