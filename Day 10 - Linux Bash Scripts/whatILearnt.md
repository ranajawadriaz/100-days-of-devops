I had to create a script that is globally available to all users to execute it. I put the script inside the scripts folder in the App server 3. The script was like creation of zip of static website xfusioncorp running inside the app server 3 and save it in the backup folder inside the app server 3 but the zip folder also had to be saved inside the backup folder inside the Nautilius Backup server also as it was temporary in the app server 3. For making the script to be able to copy the zip into the backup server without the password thing, i had to generate a key inside the app server 3 and copied its ssh id to nautilius backup server.